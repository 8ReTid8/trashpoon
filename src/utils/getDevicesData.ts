"use server"

export function getDevicesData() {
    try {
        const res = fetch("", {
            headers: {
                
            }
        })
    } catch (error) {
        console.log(error);
    }
}